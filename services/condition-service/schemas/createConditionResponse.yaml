# SPDX-License-Identifier: MIT

title: CreateConditionResponse
type: object
additionalProperties: false
required: [name, owner, address]
description: Create condition response

properties:
  name:
    type: string
    description: Name of the created condition
  owner:
    $ref: "../../../shared/schemas/address.yaml"

  address:
    type: string
    description: Deployed address placeholder (currently "0x0" in chain mode).
