# SPDX-License-Identifier: MIT

title: AuthResponse
type: object
additionalProperties: false
required: [access_token]
description: Auth response.

properties:
  access_token:
    type: string
    description: JWT access token for protected chain endpoints.
