# SPDX-License-Identifier: MIT

title: CreateConnectorResponse
type: object
additionalProperties: false
required: [name, owner, address, format_hash]
description: Connector publish response payload.

properties:
  name:
    type: string
    description: Connector name.

  owner:
    $ref: "../../../shared/schemas/address.yaml"

  address:
    type: string
    description: Deployed address placeholder (currently "0x0" in chain mode).

  format_hash:
    type: string
    description: Derived format hash for this connector definition.
