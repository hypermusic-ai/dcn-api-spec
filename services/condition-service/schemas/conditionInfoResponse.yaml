# SPDX-License-Identifier: MIT

title: ConditionInfoResponse
type: object
additionalProperties: false
required: [address, owner, name, sol_src]
description: Condition information

properties:
  address: { $ref: "../../../shared/schemas/address.yaml" }

  owner: { $ref: "../../../shared/schemas/address.yaml" }

  name:
    type: string
    description: Condition name

  sol_src:
    type: string
    description: Solidity source
